import React from 'react';
import ReactDOM from 'react-dom';
import { PToColor } from './App';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <PToColor />
  </React.StrictMode>,
  document.getElementById('root'),
);
